using System.Collections.Generic;

public class  MinStepsForEqualPileProgram
{
    /*
    Alexa is given n piles of equal or unequal heights. 
    In one step, Alexa can remove any number of boxes from the pile which has the maximum height 
    and try to make it equal to the one which is just lower than the maximum height of the stack.
     Determine the minimum number of steps required to make all of the piles equal in height.
    */
    // public int GetNumberOfSteps(int[] boxes) 
    // {
    //     if (boxes == null)
    //         return 0;

    //     SortedSet<int> sorted = new SortedSet<int>(boxes);
    //     int counter = 0, max = 0;

    //     while (sorted.Count > 0) {
    //         max = sorted.Max;
    //         sorted.Remove(max);
            
    //     }
    // }
}